<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.appbar.MaterialToolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/appToolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/brand_brown"
    android:theme="@style/ThemeOverlay.Material3.Dark.ActionBar"
    app:title=""
    app:titleCentered="false"
    app:menu="@menu/menu_top"
    app:contentInsetStart="16dp"
    app:contentInsetEnd="16dp">

    <!-- Logo chico, fijo a la izquierda -->
    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="start|center_vertical"
        android:contentDescription="@string/app_name"
        android:src="@drawable/logo_lamontana"
        android:adjustViewBounds="true"
        android:scaleType="centerInside" />

    <!-- Centro reservado para el futuro "user chip" -->
    <FrameLayout
        android:id="@+id/user_chip_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <!-- Ancla que ya usabas (si lo necesitás para algún PopupMenu propio) -->
    <View
        android:id="@+id/action_nav_anchor"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_gravity="end" />
</com.google.android.material.appbar.MaterialToolbar>
