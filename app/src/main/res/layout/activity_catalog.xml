<?xml version="1.0" encoding="utf-8"?>
<!--
 =============================================================
 Archivo: activity_catalog.xml
 Ubicación: app/src/main/res/layout/

 ¿De qué se encarga este archivo?
   - Define la interfaz de la pantalla principal de la app “La Montaña”:
     el **Catálogo de productos y servicios**.
   - Permite al usuario ver las opciones disponibles (impresiones, anillados, etc.),
     filtrarlas por tipo, agregar productos al carrito y visualizar un resumen rápido.

 Relación con las clases:
   - Controlada por la clase MainActivity.java, que:
       ▪ Carga los datos simulados (mock) de productos.
       ▪ Renderiza dinámicamente los ítems dentro del contenedor llCatalogContainer.
       ▪ Gestiona los botones de filtro y acciones del carrito.

 Estructura general:
   1. AppBar (include_app_bar.xml): barra superior reutilizable con logo y título.
   2. Panel de carrito superior: muestra cantidad, total y botones “Ver” / “Vaciar”.
   3. Título de la pantalla (tvTitle).
   4. Filtros de categoría (todo / impresiones / anillados).
   5. Título de catálogo.
   6. Lista scrolleable del catálogo con items inflados dinámicamente.
   7. Footer (include_footer.xml) con datos institucionales o de contacto.

 Diseño:
   - Usa ConstraintLayout para asegurar una estructura adaptable.
   - ScrollView para recorrer todo el listado de productos verticalmente.
   - Colores, márgenes y tipografías definidos en recursos (res/values/).
   - Fondo: @color/brand_background, coherente con la identidad visual.

 =============================================================
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/spacing_md"
    android:background="@color/brand_background">

    <include
        android:id="@+id/navbar"
        layout="@layout/include_navbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!--
        Panel superior del carrito:
        - Muestra cantidad de ítems y total general.
        - Permite ver el carrito completo o vaciarlo.
        - Se actualiza dinámicamente desde MainActivity.updateCartUi().
    -->
    <LinearLayout
        android:id="@+id/panelCart"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_sm"
        android:background="@drawable/bg_card"
        app:layout_constraintTop_toBottomOf="@+id/navbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Encabezado del carrito -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:baselineAligned="false">

            <TextView
                android:id="@+id/tvCartTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/cart_title"
                android:textStyle="bold"
                android:textSize="@dimen/text_lg"
                android:textColor="@color/brand_text_primary" />

            <!-- Badge de cantidad de ítems -->
            <TextView
                android:id="@+id/tvCartCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="(0 ítems)"
                android:textColor="@color/brand_text_primary"
                android:layout_marginEnd="@dimen/spacing_sm" />
        </LinearLayout>

        <!-- Total del carrito + botones de acción -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="@dimen/spacing_sm">

            <!-- Total acumulado -->
            <TextView
                android:id="@+id/tvTotal"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/cart_total_placeholder"
                android:textStyle="bold"
                android:textColor="@color/brand_text_primary" />

            <!-- Botones: Ver carrito y Vaciar -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnViewCart"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/view_cart"
                android:layout_marginEnd="@dimen/spacing_sm" />

            <com.google.android.material.button.MaterialButton
                style="@style/Widget.Parcial1.DangerButton"
                android:id="@+id/btnClearCart"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cart_clear" />
        </LinearLayout>
    </LinearLayout>

    <!--
        Título general de la pantalla, separado del panel del carrito.
    -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/screen_title"
        android:textSize="@dimen/text_xl"
        android:textStyle="bold"
        android:textColor="@color/brand_text_primary"
        android:layout_marginTop="@dimen/spacing_sm"
        app:layout_constraintTop_toBottomOf="@id/panelCart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        Botones de filtro por categoría de producto.
        Se conectan con los listeners en MainActivity para filtrar la lista.
    -->
    <LinearLayout
        android:id="@+id/llFilters"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="@dimen/spacing_sm"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFilterAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/filter_all"
            android:layout_marginEnd="@dimen/spacing_xs" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFilterPrint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/filter_print"
            android:layout_marginEnd="@dimen/spacing_xs" />

<!--        boton de servicios -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFilterBinding"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/filter_binding" />
    </LinearLayout>

    <!--
        Subtítulo que introduce el listado del catálogo.
    -->
    <TextView
        android:id="@+id/tvCatalogTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/catalog_title"
        android:textStyle="bold"
        android:textSize="@dimen/text_lg"
        android:textColor="@color/brand_text_primary"
        android:layout_marginTop="@dimen/spacing_sm"
        app:layout_constraintTop_toBottomOf="@id/llFilters"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        Footer común reutilizable (datos de contacto / branding).
        Permite mantener consistencia entre pantallas.
    -->
    <include
        android:id="@+id/includeFooter"
        layout="@layout/include_footer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!--
        Catálogo de productos scrolleable:
        MainActivity infla dinámicamente los ítems (item_catalog.xml)
        dentro del LinearLayout llCatalogContainer.
    -->

    <!-- ===== Overlay para cerrar el menú tocando afuera ===== -->
    <ScrollView
        android:id="@+id/svCatalog"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/includeFooter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvCatalogTitle"
        app:layout_constraintVertical_bias="0.0">

        <LinearLayout
            android:id="@+id/llCatalogContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/spacing_lg" />
    </ScrollView>

    <View
        android:id="@+id/overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#88000000"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== Menú deslizante desde arriba (Top Sheet / se activa tocando el NavBar) ===== -->
    <include
        android:id="@+id/topSheet"
        layout="@layout/layout_top_sheet_menu"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
